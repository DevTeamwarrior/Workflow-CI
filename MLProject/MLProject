name: Proyek_Retraining_Bunga

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      learning_rate: {type: float, default: 0.0005}
      epochs: {type: int, default: 15}
    command: "python modelling.py --learning_rate {learning_rate} --epochs {epochs}"